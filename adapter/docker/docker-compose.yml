version: '3.8'
services:
  app:
    build: .
    image: adolphnov/gptworker:latest
    ports:
      - "8787:8787"
    volumes:
      - ./config:/app/config
    environment:
      - DOMAIN=${DOMAIN}
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    restart: unless-stopped
  redis:
    image: "redis:alpine"
    restart: unless-stopped